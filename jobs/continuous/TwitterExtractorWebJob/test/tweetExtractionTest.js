"use strict"

let assert = require('assert');

describe('Twitter Extraction Validation', () => {
    it('validate that a tweet is received and matches the expected payload', done => {
        require('../index');
    });
});